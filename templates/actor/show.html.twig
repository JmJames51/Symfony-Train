{% extends "base.html.twig" %}

{% block body %}

	<h1>
		{{actor.name}}
	</h1>
	{% for program in actor.programs %}
		<li>
			{{program.title}}
		</li>
	{% else %}
		<li>
			Cet acteur ne joue dans aucune série
		</li>
	{% endfor %}
{% endblock %}
