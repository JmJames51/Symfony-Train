{% extends "base.html.twig" %}

{% block body %}

	<h1>
		{{actor.name}}
	</h1>
	{% for program in actor.programs %}
		<li>
			<a href="{{ path("program_show", { slug: program.slug }) }}">{{program.title}}</a>
		</li>
	{% else %}
		<li>
			Cet acteur ne joue dans aucune série
		</li>
	{% endfor %}
{% endblock %}
