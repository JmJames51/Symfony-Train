{% extends 'base.html.twig' %}
{% block body %}
	<div class="media">
		<h1 class="mt-0">{{ program.title }}</h1>
        <p>Voici tout les épisodes de la saison {{ season.number }} de {{ program.title }}</p>
		{% for episode in season.episodes %}
			<div class="media-body">
				<a href="{{ path('program_show_episode', { slug: program.slug, season_id: season.id, episode_id: episode.id}) }}">Episode : {{episode.number}}</a>
				<p>Synopsis : {{episode.synopsis}}</p>
			</div>
		{% endfor %}
			<a href="{{ path("program_show", { slug: program.slug }) }}">Retour sur la fiche de {{ program.title }}</a>
	</div>
{% endblock %}
