{% extends 'base.html.twig' %}
{% block body %}
	{% for categories in category %}
		<div>
			<h2>
				<a href='/category/{{ categories.name }}'>{{ categories.name }}</a>
			</h2>
		</div>
		<div>
			<a href="{{ path("category_new")}}">
				Ajouter une nouvelle categorie</a>
		</div>
	{% endfor %}
{% endblock %}
